{% extends 'base.html.twig' %}

{% block title %}Edition
{% endblock %}

{% block body %}
	<h1>Editer Compétence</h1>

	{{ form_start(form) }}
	{{ form_widget(form) }}

	<button class="btn btn-primary btn-sm" type="submit">Sauvegarder</button>
	<a class="btn btn-primary btn-sm" href="{{ path('app_back_admin_skill') }}">Retour Liste</a>
	
	{{ include('back/skill/_delete.html.twig') }}
	<button type="button" class="btn btn-danger btn-sm" data-toggle="modal" data-target="#deleteModal{{ skill.id }}">
		<i class="mdi mdi-delete"></i>
		Supprimer
	</button>

	{{ form_end(form) }}


{% endblock %}
