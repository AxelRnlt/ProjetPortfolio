<div class="modal fade" id="deleteModal{{ skill.id }}" tabindex="-1" role="dialog" aria-labelledby="deleteModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="deleteModalLabel">SUPPRIMER</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
       Êtes-vous sûr de supprimer: {{ skill.name }} ?
      </div>
      <div class="modal-footer">
        <a href="{{ path('app_back_admin_skill_delete', {'id': skill.id}) }}" class="btn btn-danger">Valider</a>
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Fermer</button>
      </div>
    </div>
  </div>
</div>

{# include param l'id pr chaque ligne de delete #}